# Build Mechanix for Debian

## Pre-requisites
Install the dependencies
    1. Nushell
        ```
        $ cargo install nushell
        ```
    2. Debootstrap

## Build Command

```
./build-debian.nu mecha-comet-m-gen1 build
```