boot*
fedora43*

# mkosi build outputs (disk images and split kernel/initrd artifacts, for
# every profile's Output= - mechanix-os, mechanix-os-qemu, mechanix-os-release)
mechanix-os*
mkosi.tools.manifest
initrd
initrd.cpio.zst

# Local secrets / machine-specific overrides - never commit these.
# See README.md "Credentials" section.
mkosi.env
mkosi.local.conf
mkosi.local/
mkosi.tools/*

mkosi.cache/*
!mkosi.cache/.gitkeep
mkosi.pkgcache/*
!mkosi.pkgcache/.gitkeep

# Scratch logs from manual `mkosi build ... | tee build.log`-style runs.
build.log
vm.log
